# 理发原型协作约定

- 使用中文沟通；项目为 Cocos Creator 3.8.8 微信小游戏，TypeScript 固定 5.4.5。
- 先读 docs/技术设计.zh.md；App 只调度，模拟、几何、输入、UI 分模块。
- 发根局部绑定，活动发束世界坐标模拟；禁止每帧整体旋转发束抹掉惯性。
- 保留切断时两侧速度、截面粗细和切口封闭。拾取使用当前模拟状态。
- 热路径复用缓存；发束不得一段一个节点/刚体。碎发数量和寿命有上限。
- 普通模式切换不重建角色和发束。UI 状态只在变化时更新，诊断不超过 5 Hz。
- 新静态文案运行 pnpm fonts:build 与 pnpm fonts:check；不手改生成字体或字形表。
- 修改代码后运行 npm test 及 npm run typecheck。不能以独立预览代替 Creator 和微信真机验收。
- 不自动启动 Creator，不截图 Creator 或其启动的预览窗口。独立浏览器预览允许验证。
- 模型和正式美术源文件不放 assets；不引入游泳项目账号、联网或缓存。
- 本阶段单人离线，无平台登录和网络同步。不得自动上传、发布或填真实微信 appid。
